// an arry of route accessable to publie
export const publicRoute = ["/"];

export const authRoute = ["/login", "/signup"];

export const apiAuthPrefix = "/api/auth";

//default redirect
export const DEFAULT_LOGIN_REDIRECT = "/setting";
